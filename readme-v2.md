# ray-hc/obsidian-fancy-extractor — Review (v2)

## Status
The project appears to be in an **active development** state, evidenced by the presence of a GitHub Actions workflow (`.github/workflows/release.yml`) and build scripts (`esbuild.config.mjs`, `package.json` scripts). The structure suggests it is intended for production use within Obsidian, but the lack of visible unit tests (beyond potential integration testing implied by CI) means its robustness outside of manual testing is unknown.

## Features
*   **Note Extraction:** Allows users to extract selected text from an Obsidian note into a new file.
*   **Custom Naming:** Supports defining default name formats using placeholders like `{WORDS:N}` (where $N$ is the number of words) and date/time variables.
*   **Stopword Filtering:** Utilizes the `stopword` module to strip common English words (e.g., "the", "and") from the initial words used in naming, improving descriptive file names.
*   **Destination Control:** Allows configuration of a relative path for extracted notes (`extracts/{DATE:YYYY-MM-DD}/`).
*   **Link Insertion:** Provides options to update the original note with a link or embed block pointing to the newly created extract (options include `link` and `embed`).
*   **Workflow Integration:** Exposes commands within Obsidian for triggering extraction, supporting both immediate execution and modal confirmation.

## Limitations
*   **Feature Scope Limitation:** The README explicitly states that merging extracted notes or using templates is not supported, suggesting these are out-of-scope features requiring PRs.
*   **Placeholder Implementation Detail:** While placeholders like `{DATE:YYYY-MM-DD}` are mentioned in the demo, the implementation details for handling all possible variables (beyond what's visible) are abstracted away and not fully clear from the provided code snippets.
*   **Error Handling Visibility:** The file moving logic includes `.catch(() => {})` blocks which silently swallow potential errors during renaming/folder creation, masking underlying issues.

## Bugs & Risks
*   **Silent Error Handling (High Risk):** In `createExtract`, using `.catch(() => {})` when renaming the file or creating folders means that if Obsidian's API fails to move the file for any reason, the user will receive no feedback other than potentially a misleading success notice.
*   **Incomplete Placeholder Logic:** The provided snippet for `getFormatWithNWords` is truncated (`... [truncated]`), making it impossible to verify the correct and comprehensive implementation of all defined placeholders (e.g., how date/time variables are actually substituted).
*   **Dependency Versioning Risk:** The use of `"obsidian": "latest"` in `package.json` can lead to unpredictable behavior if a major Obsidian API change occurs without corresponding updates to this plugin's dependencies or logic.

## Summary
This is a functional, customizable Obsidian plugin designed to streamline the process of extracting selected text into new, uniquely named notes within specific directories. It handles name formatting using stopwords and supports updating the source note with appropriate markdown links. However, the code contains several instances of silent error handling and relies on external placeholder logic that cannot be fully verified from the provided context.

---
*Generated by repo-reviewer using Ollama model `gemma4:latest` on 2026-07-29.*
